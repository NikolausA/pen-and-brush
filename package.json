{
  "name": "protected-notes",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run fe:build && npm run be:build",
    "fe:build": "npm -w frontend run build",
    "be:build": "npm -w backend run build"
  },
  "engines": {
    "node": "22"
  }
}
